use this to create the env
    conda create -n qasr_vtu_viewer python=3.11 pyside6 vtk numpy pip

to activate
    conda activate qasr_vtu_viewer

to install requirements
    pip install -r requirements.txt